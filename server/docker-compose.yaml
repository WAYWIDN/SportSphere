services:
  # mongo:
  #   image: mongo:latest
  #   container_name: sport_sphere_mongo
  #   ports:
  #     - "27017:27017"
  #   environment:
  #     MONGO_ININTDB_DATABSE : sport_sphere_mongo
  #   volumes:
  #     - mongo_data:/data/db
  redis:
    image: redis:latest
    container_name: sport_sphere_redis
    command: ["redis-server","--appendonly","yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  # mongo_data:
  redis_data:
